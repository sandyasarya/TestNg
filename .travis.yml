language: java
install: true

cache:
  directories:
    - $HOME/.m2
   
jdk: oraclejdk8


script:
- mvn -f ${TRAVIS_BUILD_DIR}/TestNgCommand1/pom.xml clean test compile package
- echo "$TRAVIS_BUILD_DIR"

notifications:
  email:
    - sandesh.gadekar@mobiliya.com
